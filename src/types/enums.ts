export enum DisplayModeEnum {
  radio = 1,
  add,
  addWithToggle,
  checkbox,
}

export enum AuthenticationTypeIdEnum {
  UserNameAndPassword = 0,
  Facebook,
  Google,
  Apple,
}

export enum WalletPaymentsEnum {
  GooglePay = 13,
  ApplyPay,
}

export enum LoyaltyHistoryTypeIdEnum {
  Consumed = 1,
  Expired,
  Collected,
  ManuallyAdded,
}

export enum HomeBannerEnum {
  banner = 1,
  bestSeller,
  featuredBanner,
  worldOfTexas,
}

export enum MediaTypeEnum {
  banner = 1,
  image,
  link,
  video,
  youtubeLink,
}

export enum Gender {
  Male = 1,
  Female,
}

export enum PrivacyRequestType {
  DoNotSellMyData = 1,
  ReceiveACopyOfMyData,
  DeleteData,
  CorrectMyData,
  AccessMyData,
}
